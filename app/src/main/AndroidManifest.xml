<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto_dmcu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PlantFriends"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TutorialVistaActivity"></activity>
        <activity android:name=".VerTutorialesActivity" />
        <activity android:name=".InformacionActivity" />
        <activity android:name=".CuidadosActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".PlantDetailsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>